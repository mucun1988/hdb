# Hamming distance between binary codes (hdb)

## Problem Statement
Given two binary codes (represented in integers), their hamming distance is the number of positions at which the corresponding bits are different.
